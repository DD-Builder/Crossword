/// <reference types="vite/client" />

/** Injected by vite.config.ts's `define` — "0.1.0+a1b2c3d" (package.json
 * version + short commit sha), fixed at build time. */
declare const __APP_VERSION__: string;
